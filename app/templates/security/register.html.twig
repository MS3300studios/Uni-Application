{% include 'default/_form.html.twig' with {
    page_title: 'title.register'|trans({'%email%': admin.email|default('')}),
    form: form,
    submit_label: 'action.register'|trans,
    back_to_list_path: 'admin_login',
} %}
